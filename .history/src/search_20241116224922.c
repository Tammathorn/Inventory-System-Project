#include <stdio.h>

int search() {

    // input and check if it has or not -- 

    // input string 
    char *string;
    printf("Enter your choice 1 for name, 2 for type");
    scanf("%s", string);

    int choice;
    scanf("%d", &choice);
    return 0;
}